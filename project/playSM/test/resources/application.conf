play {
  http.secret.key = "secret"

  i18n.langs = ["en"]

  assets {
    path = "/public"
    urlPrefix = "/assets"
  }
}

# Database configuration
# ~~~~~
slick.dbs.default {
  profile = "utils.db.SmPostgresDriver$"

  db {
    profile = "org.postgresql.Driver"
    url = "jdbc:postgresql://localhost:55553/play_sm_db_test"
    user = "play_sm_user"
    password = "123"

    # s.u.AsyncExecutor - Having maxConnection > maxThreads can result in deadlocks if transactions or database locks are used.
    numThreads = 20
    maxConnections = 20
  }
}


# fixtures
db.default.fixtures.scriptLocation="db/fixtures/default"
db.default.fixtures.scriptPackage="ru.ns.fixtures"
db.default.fixtures.scripts=["sm_device.sql", "sm_file_card.sql", "sm_path_move.sql", "MyFixtureScript"]
