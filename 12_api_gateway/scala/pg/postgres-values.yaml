global:
  postgresql:
    auth:
      postgresPassword: "postgres"

auth:
  username: "authuser"
  password: "authpasswd"
  database: "authdb"

primary:
  initdb:
    user: "authuser"
    scripts:
        test.sh: |
            #!/bin/sh
            echo "Hello world!"
  persistence:
    enabled: true
    size: 100m

  resources:
    requests:
      memory: 256Mi
      cpu: 250m

  # service:
  #   type: NodePort
  #   port: 5432

networkPolicy:
  enabled: "true"

  metrics:
    enabled: false
    serviceMonitor:
      enabled: false
